import { defineStore } from 'pinia';
// import { useLocalStorage } from '@vueuse/core';

export const useBasketStore = defineStore('basket', () => {
  // const basket = useLocalStorage('basket', [] as BasketProductInterface[]);

  return {
    // basket,
  };
});
